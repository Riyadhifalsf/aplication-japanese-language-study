import 'package:flutter/material.dart';

/// Ads are intentionally removed from the product UI.
class AdSlot extends StatelessWidget {
  const AdSlot({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
